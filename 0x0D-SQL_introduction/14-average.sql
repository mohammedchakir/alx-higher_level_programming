-- This SQL script computes the score average of all records in the table 'second_table' of the database 'hbtn_0c_0'.
-- The result column is named 'average'.
SELECT AVG(score) AS average FROM second_table;
